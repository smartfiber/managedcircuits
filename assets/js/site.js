const navToggle=document.querySelector('.nav-toggle');const nav=document.querySelector('.nav-links');if(navToggle&&nav){navToggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');navToggle.setAttribute('aria-expanded',String(open));});}
document.querySelectorAll('.portal-tab').forEach(tab=>tab.addEventListener('click',()=>{document.querySelectorAll('.portal-tab').forEach(t=>t.classList.remove('active'));document.querySelectorAll('.portal-view').forEach(v=>v.classList.remove('active'));tab.classList.add('active');document.getElementById(tab.dataset.view).classList.add('active');}));
const form=document.querySelector('#rate-form');if(form){let step=1;const max=5;const showStep=n=>{step=n;document.querySelectorAll('.form-step').forEach(s=>s.classList.toggle('active',Number(s.dataset.step)===n));document.querySelectorAll('.step-link').forEach((s,i)=>{s.classList.toggle('active',i+1===n);s.classList.toggle('done',i+1<n)});window.scrollTo({top:0,behavior:'smooth'});};document.querySelectorAll('[data-next]').forEach(b=>b.addEventListener('click',()=>showStep(Math.min(max,step+1))));document.querySelectorAll('[data-prev]').forEach(b=>b.addEventListener('click',()=>showStep(Math.max(1,step-1))));document.querySelectorAll('.choice,.contract-choice').forEach(c=>c.addEventListener('click',()=>{const group=c.parentElement;c.classList.toggle('selected');if(c.classList.contains('contract-choice'))group.querySelectorAll('.contract-choice').forEach(o=>{if(o!==c)o.classList.remove('selected')});}));form.addEventListener('submit',e=>{e.preventDefault();document.querySelector('.success-message').classList.add('show');});showStep(1);}
